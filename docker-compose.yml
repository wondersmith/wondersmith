version: "3"

services:
  database:
    image: "postgres:latest"
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: wonder
      POSTGRES_PASSWORD: smith
      POSTGRES_DB: wondersmith
